<% if current_user.projects.primary_owner.include?(@project) %>
    <%= button_to "Edit Project Details", edit_project_path, method: "get", :class => "btn btn-outline-primary"  %>  <%= button_to "Add a New Goal", new_goal_path, method: "get", :class => "btn btn-outline-primary"  %>  <%= button_to "Add a New Collaborator", new_collaboration_path, method: "get", :class => "btn btn-outline-primary"  %>
<% end %>

<% if current_user.projects.owner.include?(@project) %>
    <%= button_to "Add a New Goal", new_goal_path, method: "get", :class => "btn btn-outline-primary"  %>
<% end %>