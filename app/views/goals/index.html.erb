goals index

<h1>My Goals</h1>

<ul>
<% @goals.each do |goal| %>
    <li><%= link_to goal.title, goal_path(goal) %>      -       <%= link_to goal.project.address, project_path(goal.project) %></li>
<% end %>
</ul>

