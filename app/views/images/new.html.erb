<h1> Add an Image for this Goal </h1>
<p> Having trouble? Upload images to Imgur and use the "Direct Link" URL. </p>

<%= render partial: "layouts/errors", locals: {object: @image} %>

<%= bootstrap_form_for(@image, url: goal_images_path) do |f| %>
    <%= f.hidden_field :id %>

    <%= f.url_field :url %>

    <%= f.text_area :caption %>

    <%= f.select  :genre, [["Inspiration", "Inspiration"], ["Design Plans", "Design Plans"], ["Before Photo", "Before Photo"], ["After Photo", "After Photo"], ["Work in Progress", "Work in Progress"]], :prompt => true %>

    <%= f.submit nil, class: 'btn btn-secondary' %>
<% end %>