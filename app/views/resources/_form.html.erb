<%= form_for(@resource, url: goal_resources_path) do |f| %>
    <%= f.hidden_field :id %>

    <%= f.label :url %>
    <%= f.url_field :url %><br>

    <%= f.label :description %>
    <%= f.text_area :description %><br>

    <%= f.submit %>
<% end %>