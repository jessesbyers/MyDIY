<h1> Add a Resource for this Goal </h1>

<%= form_for(@resource, url: goal_resources_path) do |f| %>
    <%= f.label :url %>
    <%= f.url_field :url %><br>

    <%= f.label :description %>
    <%= f.text_area :description %><br>

    <%= f.submit %>
<% end %>